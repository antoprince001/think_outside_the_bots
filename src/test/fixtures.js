import { presets } from '../workflows/presets';
export const connection = { id: 'fixture-model', label: 'Fixture model', provider: 'openai', model: 'gpt-4.1-mini' };
export const workflow = presets[0];
